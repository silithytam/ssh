name: CI

on: [push, workflow_dispatch]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - name: Download ngrok
      run: |
        wget https://bin.equinox.io/c/bNyj1mQVY4c/ngrok-v3-stable-linux-amd64.tgz
        tar xvzf ngrok-v3-stable-linux-amd64.tgz
        chmod +x ngrok
    
    - name: Auth ngrok
      run: ./ngrok authtoken 2baVR4YQ0Dy9Ilr8TUhQhQiSost_3jWMbnXzdFJf7M5S61iRa
    
    - name: Setup XRDP
      run: |
        sudo apt-get update
        sudo apt-get install -y xrdp xfce4 xfce4-goodies jq
        sudo systemctl start xrdp
        sudo systemctl enable xrdp
        sudo adduser xrdp ssl-cert
    
    - name: Configure XRDP
      run: |
        echo "xfce4-session" > ~/.xsession
        sudo sed -i 's/^new_cursors=true/new_cursors=false/g' /etc/xrdp/xrdp.ini
        sudo systemctl restart xrdp
    
    - name: Set User Password
      run: |
        echo "runner:P@ssw0rd!" | sudo chpasswd
    
    - name: Start ngrok and display URL
      run: |
        ./ngrok tcp 3389 > /dev/null &
        sleep 10
        
        NGROK_URL=$(curl -s http://127.0.0.1:4040/api/tunnels | jq -r '.tunnels[0].public_url')
        NGROK_HOST=$(echo $NGROK_URL | sed 's|tcp://||' | cut -d':' -f1)
        NGROK_PORT=$(echo $NGROK_URL | sed 's|tcp://||' | cut -d':' -f2)
        
        echo "=========================================="
        echo "RDP Connection Info:"
        echo "Host: $NGROK_HOST"
        echo "Port: $NGROK_PORT"
        echo "Username: runner"
        echo "Password: P@ssw0rd!"
        echo ""
        echo "Connection Command (Linux):"
        echo "xfreerdp /v:$NGROK_HOST:$NGROK_PORT /u:runner /p:P@ssw0rd!"
        echo ""
        echo "For Windows: Use Remote Desktop Connection"
        echo "=========================================="
        
        sleep 6h
